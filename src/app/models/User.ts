export interface User {
  id: string;
  username: string;
  first_name: string;
  last_name: string;
  email: string;
  city: string;
  country: string;
  bio: string;
  accessToken: string;
}

// TODO: create the builder class for the interface instantiation?
